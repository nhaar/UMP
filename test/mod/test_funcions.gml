/// FUNCTIONS

function test_fn2()
{
    return test_fn1();
}

function test_fn1()
{
    return 0;
}

function test_fn7 (something, more, complex)
{
    show_debug_message(something)
    show_debug_message(more)
    show_debug_message(complex)
    putting_more_code_here = false

    if (something)
    {
        return true
    }
    else
    {
        return false
    }
    // one of the tests of all time
    return 0
}